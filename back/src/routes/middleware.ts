/* import { Router } from 'express';
import Middleware from '../controller/Middleware';

const auth = require('../middleware/auth');

export const middlewareRoutes = Router();

//userRoutes.get('/user', user.get);
middlewareRoutes.post('/auth', auth, Middleware.post); */
//pointRoutes.get('/points', Middleware.get);
//pointRoutes.get('/points', Sport.get);
//userRoutes.post('/user/logout', user.logout);
//userRoutes.get('/user/id', user.getUsersById);
//userRoutes.delete('/user', user.delete);
//userRoutes.put('/user', user.put);